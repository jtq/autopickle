#$gherkin_root_dir = "/path/to/your/cucumber/tests/directory"	# Set default root directory for gherkin step_definitions and feature files (overridable by --directory command-line param)
#$step_definition_lang = "ruby"	# Set default language step_definitions are written in (overridable by --language command-line param)

# Optionally define your own languages if none of the built-in ones are appropriate - see README.md for more information
#$lang_config["ruby"] = LangConfig.new(/^\s*(?:given|when|then|and|but)\s*\(?\s*\/(\^?[^$\n]*\$?)\/\s*\)?\s*(?:(?:do|{)\s*\|?([^|\n]*)\|?)/i, nil, /\s*,\s*/, "/**/*.rb", "/**/*.feature")
